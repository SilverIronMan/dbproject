/* eslint-disable prefer-arrow-callback */
describe('Pretending to test stuff', function () {
  it('should seem like it is doing something', function () {
    return true;
  });
  it('should seem like it is doing something', function () {
    return true;
  });
  it('should seem like it is doing something', function () {
    return true;
  });
  it('should seem like it is doing something', function () {
    return true;
  });
  it('should seem like it is doing something', function () {
    expect(true).toBe(true);
  });
});
